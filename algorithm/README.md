# Algorithm

Python pathfinding service (Flask REST API).

Responsibilities:
- A* search for optimal robot paths
- TSP/DP for obstacle visit order
- Path → STM32 command string conversion

Run: `python app.py` (port 5000)
